<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <h2>Wikis</h2>
    <% if policy(@wikis).new? %>
      <%= link_to "New Wiki", new_wiki_path, class: 'btn btn-success' %>
    <% end %>
    <%= render @wikis %>
  </div>
</div>
